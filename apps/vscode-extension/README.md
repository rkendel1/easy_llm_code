# easy-llm-code for VS Code

The VS Code sidebar for easy-llm-code. It starts the local authenticated runtime for the current workspace and provides chat, task history, progress, approvals, diffs, and provider controls without sending project memory to an extension service.

Install the `easy-llm-code` CLI first, open a folder, then open the easy-llm-code activity-bar view.
